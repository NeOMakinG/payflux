export enum BlockType {
	CONDITION = 'Condition',
	FUNCTION = 'Function',
	START = "Start",
	END = "End",
}

export enum Conditions {
	IF_ELSE = 'IF_ELSE',
	SWITCH = 'SWITCH',
}

export enum Functions {
	ADD = 'ADD',
	SUBTRACT = 'SUBTRACT',
	MULTIPLY = 'MULTIPLY',
	DIVIDE = 'DIVIDE',
}
