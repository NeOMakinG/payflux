export enum BlockType {
    CONDITION = 'CONDITION',
    FUNCTION = 'FUNCTION',
}

export enum Conditions {
    IF_ELSE = 'IF_ELSE',
    SWITCH = 'SWITCH',
}

export enum Functions {
    ADD = 'ADD',
    SUBTRACT = 'SUBTRACT',
    MULTIPLY = 'MULTIPLY',
    DIVIDE = 'DIVIDE',
}
